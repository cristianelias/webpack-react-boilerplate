// Dependencies
import styled from "@emotion/styled";

const Heading = styled.h1`
  font-size: 64px;
  text-align: center;
`

const App = () => {
  return <Heading>hi</Heading>;
};

export default App;
